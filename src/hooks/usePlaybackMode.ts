/**
 * Reactive playback-mode hook.
 * Implementation now lives in the setting factory; re-exported here so the
 * hook stays discoverable under /hooks.
 */
export { usePlaybackMode } from "@/config/playback";
